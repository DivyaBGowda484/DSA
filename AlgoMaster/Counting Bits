Question 338: Counting Bits (https://leetcode.com/problems/counting-bits)

class Solution:
    def countBits(self, n: int) -> List[int]:
        '''ans = [0] * (n+1)
        for i in range(n+1):
            count = 0
            bin_rep = bin(i) # log i time
            for j in bin_rep:
                if j == '1':
                    count += 1
            ans[i] = count
        return ans'''

# TIME COMPLEXITY: O(n logn)
# SPACE COMPLEXITY: O(n)

# OPTIMIZED APPROCH: # did not understand
        ans = [0] * (n + 1)
        for i in range(1, n + 1):
            ans[i] = ans[i >> 1] + (i & 1)
        return ans

# TIME COMPLEXITY: O(n)
# SPACE COMPLEXITY: O(n)

