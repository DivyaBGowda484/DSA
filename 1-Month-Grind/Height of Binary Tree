Question: Height of Binary Tree (https://www.geeksforgeeks.org/problems/height-of-binary-tree/1)

'''
# Node Class:
class Node:
    def __init__(self,val):
        self.data = val
        self.left = None
        self.right = None
'''
class Solution:
    #Function to find the height of a binary tree.
    def height(self, root):
        # code here
    
# Recursive Solution:
        if root is None:
            return -1  # return -1 for empty, 0 for leaf
    
        left_height = self.height(root.left)
        right_height = self.height(root.right)
    
        return 1 + max(left_height, right_height)

# TIME COMPLEXITY: O(n)
# SPACE COMPLEXITY: O(h)

# Iterative - Queue:
        if root is None:
            return -1

        queue = deque([root])
        height = -1

        while queue:
            for _ in range(len(queue)):
                node = queue.popleft()
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            height += 1

        return height
        
# TIME COMPLEXITY: O(n)
# SPACE COMPLEXITY: O(n)
