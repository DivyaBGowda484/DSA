Question: Weather Observation Station 10 (https://www.hackerrank.com/challenges/weather-observation-station-10/problem?isFullScreen=true)

select distinct city
from station
where city regexp '[^AEIOU]$'

/*
SELECT DISTINCT CITY
FROM STATION
WHERE CITY NOT LIKE '%A'
  AND CITY NOT LIKE '%E'
  AND CITY NOT LIKE '%I'
  AND CITY NOT LIKE '%O'
  AND CITY NOT LIKE '%U';
*/
