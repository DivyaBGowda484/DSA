Question: Check for Prime

def checkPrime(n):
    cnt = 0

    for i in range(1, int(n ** 0.5) + 1):
        if n % i == 0:
            cnt += 1
            if n // i != i:
                cnt += 1

    return cnt == 2

# TIME COMPLEXITY: O(sqrt(N)
# SPACE COMPLEXITY: O(1)
