"""A wire connects N light bulbs. Each bulb has a switch associated with it; however, due to faulty wiring, a switch also changes the state of all the bulbs to the right of the current bulb.
Given an initial state of all bulbs, find the minimum number of switches you have to press to turn on all the bulbs.
You can press the same switch multiple times. 
Note: 0 represents the bulb is off and 1 represents the bulb is on."""


# To take user input of current state of bulbs:
A = list(map(int, input("Enter space sperated inital bulb states(0,1): ").split()))


# To find minimum number of switches to turn all bulbs on:
def min_switches_brute(A):
    N = len(A) #S: O(1)
    count = 0

    for i in range(N):
        if A[i] == 0:
            count += 1
            for j in range(i, N):
                A[j] = 1 - A[j]
    return count
    
brute_result = min_switches_brute(A.copy())
print("Brute-force Result:", brute_result)

# TIME COMPLEXITY:O(n^2)
# SPACE COMPLEXITY:O(1)


# OPTIMIZED CODE:
def min_switches_optimized(A):
    flip = 0
    count = 0

    for bulb in A:
        effective_state = bulb if flip % 2 == 0 else 1 - bulb
        if effective_state == 0:
            count += 1
            flip += 1
    return count

optimized_result = min_switches_optimized(A.copy())
print("Optimized Result:", optimized_result)


"""
| Index | Bulb | Flip So Far | Flip % 2 | Real (Effective) State | Switch Needed? | Count | Flip Now   |
|-------|------|-------------|----------|-------------------------|----------------|--------|----------|
| 0     | 1    | 0           | 0        | 1 (no flip)             | ❌ No          | 0      | 0        |
| 1     | 0    | 0           | 0        | 0 (no flip)             | ✅ Yes         | 1      | 1        |
| 2     | 0    | 1           | 1        | 1 (flipped)             | ❌ No          | 1      | 1        |
| 3     | 1    | 1           | 1        | 0 (flipped)             | ✅ Yes         | 2      | 2        |
| 4     | 1    | 2           | 0        | 1 (no flip)             | ❌ No          | 2      | 2        |
| 5     | 0    | 2           | 0        | 0 (no flip)             | ✅ Yes         | 3      | 3        |
| 6     | 1    | 3           | 1        | 0 (flipped)             | ✅ Yes         | 4      | 4        |
| 7     | 0    | 4           | 0        | 0 (no flip)             | ✅ Yes         | 5      | 5        |
"""
