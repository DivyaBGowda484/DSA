Question 445: Add Two Numbers II (https://leetcode.com/problems/add-two-numbers-ii)

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
# BRUTE FORCE:
        """def reverseList(head):  
            prev = None
            while head:
                head.next, prev, head = prev, head, head.next
            return prev

        # Reverse both input lists
        l1 = reverseList(l1)
        l2 = reverseList(l2)

        # Add the numbers
        carry = 0
        dummy = ListNode(0)
        curr = dummy

        while l1 or l2 or carry:
            val1 = l1.val if l1 else 0
            val2 = l2.val if l2 else 0
            total = val1 + val2 + carry

            carry = total // 10
            curr.next = ListNode(total % 10)
            curr = curr.next

            if l1: l1 = l1.next
            if l2: l2 = l2.next

        # Reverse the result to restore MSB first order
        return reverseList(dummy.next)

# TIME COMPLEXITY: O(n + m)
# SPACE COMPLEXITY: O(1)"""

# OPTIMIZED CODE:
        # Use two stacks to store the digits of l1 and l2
        stack1, stack2 = [], []

        # Push all digits of l1 into stack1
        while l1:
            stack1.append(l1.val)
            l1 = l1.next
        
        # Push all digits of l2 into stack2
        while l2:
            stack2.append(l2.val)
            l2 = l2.next
        
        carry = 0
        head = None  # We will build the result in reverse (inserting at head)

        while stack1 or stack2 or carry:
            val1 = stack1.pop() if stack1 else 0
            val2 = stack2.pop() if stack2 else 0
            total = val1 + val2 + carry

            carry = total // 10
            node = ListNode(total % 10)
            node.next = head
            head = node

        return head

# TIME COMPLEXITY: O(n + m)
# SPACE COMPLEXITY: O(n + m)
